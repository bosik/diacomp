<?xml version="1.0" encoding="utf-8"?>
<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">
	<PreferenceCategory android:title="Синхронизация">
		<EditTextPreference
			android:name="prefServer"
			android:title="@string/prefLabelServer"
			android:summary=""
			android:defaultValue="@string/prefDefaultServer"			
			android:key="@string/prefServer" />
		<EditTextPreference
			android:name="prefUsername"
			android:title="@string/prefLabelUsername"
			android:summary=""
			android:defaultValue="@string/prefDefaultUsername"			
			android:key="@string/prefUsername" />
		<EditTextPreference
			android:name="prefPassword"
			android:title="@string/prefLabelPassword"
			android:summary=""
			android:defaultValue="@string/prefDefaultPassword"			
			android:key="@string/prefPassword" />
	</PreferenceCategory>
</PreferenceScreen>